import {BudgetCard, Text} from '@/components';
import {View} from 'react-native';

type Props = {};

export default function Budget({}: Props) {
  return (
    <View>
    <BudgetCard />
    </View>
  );
}
